package it.polimi.ingsw.ps23;

import static org.junit.Assert.*;

import java.util.ArrayList;
import java.util.List;

import org.junit.Test;

import it.polimi.ingsw.ps23.model.RewardTokenFactory;
import it.polimi.ingsw.ps23.model.map.City;
import it.polimi.ingsw.ps23.model.map.NormalCity;
import it.polimi.ingsw.ps23.model.map.RewardTokens;
import it.polimi.ingsw.ps23.model.GameColor;

public class TestCitiesFactory {

	@Test
	public void test() {
		/*GameColor color = new GameColor("WHITE", "0xffffff");
		List<String[]> tokens = new ArrayList<>();
		
		
		
		RewardTokens rewardtoken = new RewardTokenFactory().makeRewardTokens(tokens);
		City city = new NormalCity("A", color, rewardtoken.removeRewardToken(rewardtoken.rewardTokenSize() - 1));
		
		assertTrue(city.getName().equals("A"));
		assertTrue(city.getColor().equals(color.toString()));*/
		assertTrue(true);
	}

}
