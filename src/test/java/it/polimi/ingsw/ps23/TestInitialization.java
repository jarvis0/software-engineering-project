package it.polimi.ingsw.ps23;

import static org.junit.Assert.*;

import org.junit.Test;

public class TestInitialization {

	@Test
	public void test() {
		/*List<String> playersID = new ArrayList<>();
		playersID.add("mirco");
		playersID.add("ale");
		Model model = null;
		model = new Model();
		model.setModel(playersID);
		/*Deck politicDeck = init.getPoliticDeck();
		FreeCouncillors freeCouncillors = init.getFreeCouncillors();
		GameMap gameMap = init.getGameMap();
		King king = init.getKing();
		KingTiles kingTiles = init.getKingTiles();
		NobilityTrack nobilityTrack = init.getNobilityTrack();
		GamePlayers gamePlayers = init.getGamePlayers();*/
		//assertTrue(model != null);
		assertTrue(true);
	}

}
